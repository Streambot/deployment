{
	"streambot_api": {
    "config": {
  		"database": {
  			"host": "#{API_REXSTER_HOST}"
  		}
    }
	},
	"aws_instance": {
		"role": "#{AWS_INSTANCE_SERVICE}",
		"env": "#{AWS_INSTANCE_ENV}"
	},
  "run_list": [
    "role[api]"
  ]
}