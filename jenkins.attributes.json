{
	"aws_instance": {
		"role": "#{AWS_INSTANCE_SERVICE}",
		"env": "#{AWS_INSTANCE_ENV}"
	},
	"graphite": {
      "write_options": {
      	"host": "#{GRAPHITE_HOST}",
      	"port": #{GRAPHITE_PORT}
      }
    },
	"run_list": [
		"role[jenkins]"
	]
}